# Git
分布式版本控制系统

## 删除远程分支：

```bash
git push [-f] <remote-repo> :<remote-branch>
```

## 查看提交日志：

```bash
git log
```

## 发布/标签

```bash
git tag -a <tag-name>
git push origin :refs/tags/<tag-name>
```
